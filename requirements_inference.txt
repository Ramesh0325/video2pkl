# Minimal requirements for PARE inference only
# For video -> SMPL .pkl inference pipeline

# Core ML
torch>=1.6.0,<2.0.0
torchvision>=0.7.0,<0.9.0
numpy>=1.19.0
scipy>=1.5.0

# Data handling
opencv-python>=4.1.0
pillow>=8.0.0
joblib>=0.15.0

# Config
pyyaml>=5.3.0
yacs>=0.1.8

# Utilities
tqdm>=4.47.0
loguru>=0.3.0

# SMPL model
smplx>=0.1.28

# Person detection and tracking (required for video inference)
# Note: These are git dependencies
# git+https://github.com/mkocabas/yolov3-pytorch.git
# git+https://github.com/mkocabas/multi-person-tracker.git

# Optional but recommended for better performance
# numba>=0.54.0  # For some utility functions

# NOTE: The following may be required for imports even if not used:
# Some modules import renderer classes (but we don't call them)
# If you get import errors, install these:
# trimesh>=3.8.0
# pyrender>=0.1.36

# NOT NEEDED for inference (removed):
# - matplotlib (visualization)
# - scikit-image (image processing, not critical)
# - albumentations (data augmentation, training only)
# - pytorch-lightning (training only)
# - fastai (training only)
# - jpeg4py (optional image loading)
# - chumpy (legacy, not needed)
# - torchgeometry (included in pare/utils)
# - filterpy (tracking, but multi-person-tracker handles it)

